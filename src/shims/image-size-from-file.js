/** Browser-safe stub for image-size fromFile (Payload upload pipeline). */
export async function imageSizeFromFile() {
  return { width: 0, height: 0, type: 'unknown' }
}

export default imageSizeFromFile
