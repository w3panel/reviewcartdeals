{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "bun install",
  "buildCommand": "bun run prebuild:with-migrate && bun run build",
  "functions": {
    "src/app/(payload)/api/**/*.ts": {
      "maxDuration": 30
    }
  }
}
